# This has the syntax of a Makefile
# Define here variables to configure CCured
# Make sure that CCUREDHOME is defined and points to the directory in which
# this file lives and the root of the CCured project. 
# If you are on a Windows machine use / as path separator
# Never check the .ccuredrc file in the repository !

ifndef _GNUCC
# If you want to use Microsoft Visual C uncomment the next line
# _MSVC = 1
endif

# If you do not want to use the garbage collector uncomment the next line
# NOGC=1


# You must set ARCHOS to either x86_WIN32 or to x86_LINUX. 
# The default is x86_LINUX
# ARCHOS=x86_WIN32

